somewhere in /tmp

$ git clone ssh://bandit31-git@localhost/home/bandit31-git/repo
$ cd repo/
$ git log
$ git show df6c5eb91615c6dc9c99f99ca5fd79addfe62594
$ ls -a
$ echo > .gitignore
$ echo "May I come in?" > key.txt
$ git add key.txt
$ git commit -m "foo"
